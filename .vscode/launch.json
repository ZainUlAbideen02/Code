{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run main.exe",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceFolder}/main.exe",
            "args": [],
            "cwd": "${workspaceFolder}",
            "console": "externalTerminal",
            "preLaunchTask": "build with g++"
        }
    ]
}
